<Page x:Class="ConnectingDBToApp.Views.Pages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartPage">
    <StackPanel Orientation="Vertical"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <Rectangle Fill="{StaticResource MouseOverColor}"
                   Width="50" Height="50">
            <Rectangle.OpacityMask>
                <VisualBrush Stretch="Uniform" Visual="{StaticResource ArrowBackIcon}"/>
            </Rectangle.OpacityMask>
        </Rectangle>
        <TextBlock Text="Выберите один из разделов"
                   Style="{StaticResource H2}"
                   Foreground="{StaticResource MouseOverColor}"/>
    </StackPanel>
</Page>
